//
//  Checkbox+Types.swift
//  DesignSystem
//

extension Checkbox {
    public enum Variant {
        case circle
        case ghost
    }

    public enum State {
        case enabled
        case disabled
    }

    public enum Size {
        case small
        case medium
    }
}
