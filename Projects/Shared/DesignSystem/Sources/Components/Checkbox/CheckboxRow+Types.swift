//
//  CheckboxRow+Types.swift
//  DesignSystem
//

extension CheckboxRow {
    public enum Size {
        case small
        case medium
    }

    public enum ArrowDirection {
        case right
        case down
        case up
    }
}
